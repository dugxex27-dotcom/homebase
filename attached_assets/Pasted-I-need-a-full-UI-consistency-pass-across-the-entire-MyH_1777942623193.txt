I need a full UI consistency pass across the entire MyHomeBase™ app. The reference design is the homeowner landing page at /homeowner and the selector at /. Do not change any content, copy, data, routing, or functionality. Only update visual styling.
THE DESIGN SYSTEM — apply these values everywhere:
Font: Inter from Google Fonts on every page and component. No other fonts.
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
font-family: 'Inter', system-ui, sans-serif;
Colors:
Homeowner primary:   #3C258E (purple)
Homeowner deep:      #2C0F5B
Homeowner light:     #B6A6F4
Homeowner tint:      #EEEDFE
Homeowner border:    #CECBF6

Contractor primary:  #1560A2 (blue)
Contractor deep:     #0C3460
Contractor light:    #AFD6F9
Contractor tint:     #E6F1FB
Contractor border:   #B5D4F4

Agent primary:       #079669 (green)
Agent deep:          #09694A
Agent light:         #D4EBDE
Agent tint:          #F0FAF4
Agent border:        #A7D7B8

Neutral gray-100:    #F3F4F6
Neutral gray-200:    #E5E7EB
Neutral gray-400:    #9CA3AF
Neutral gray-600:    #4B5563
Neutral gray-900:    #111827
Error red:           #DC2626
Success green:       #079669
Warning amber:       #D97706
COMPONENT SPECS:
Sidebar nav:

Background: white, border-right: 1px solid #E5E7EB
Logo: 28px tall, links to dashboard
Nav items: Inter 14px, font-weight 500, color #4B5563, padding 10px 16px, border-radius 8px
Active nav item: background = user's primary color tint, color = user's primary color, font-weight 600
Active indicator: 3px left border in user's primary color
Sign out: color #DC2626

Page header banners (the colored section at top of each logged-in page):

Homeowner pages: gradient from #2C0F5B to #3C258E
Contractor pages: gradient from #0C3460 to #1560A2
Agent pages: gradient from #09694A to #079669
Eyebrow label: Inter 10px, weight 600, letter-spacing 2px, uppercase, color rgba(255,255,255,0.6)
Page title: Inter 24–28px, weight 700, color white
Subtitle: Inter 14px, color rgba(255,255,255,0.65)
Stat tiles within banner: background rgba(255,255,255,0.12), border 0.5px solid rgba(255,255,255,0.2), border-radius 12px, padding 14px 18px
Stat number: Inter 22px, weight 700, color white
Stat label: Inter 10px, uppercase, letter-spacing 1px, color rgba(255,255,255,0.55)

Cards:

Background: white
Border: 0.5px solid #E5E7EB
Border-radius: 16px
Box-shadow: 0 2px 12px rgba(0,0,0,0.06)
Padding: 20px 24px
Card section header: Inter 15px, weight 600, color #111827

Buttons:

Primary: background = user's primary color, color white, border-radius 10px, padding 11px 20px, font-weight 600, font-size 14px, border none
Secondary: background transparent, border 1.5px solid user's primary color, color = user's primary color, same radius/padding
Destructive: background #DC2626, color white, same radius/padding
Small variant: padding 7px 14px, font-size 13px

Form inputs:

Padding: 11px 14px
Border: 1.5px solid #E5E7EB
Border-radius: 10px
Font-size: 14px
Focus border-color: user's primary color
Background: white

Section labels / eyebrows:

Font-size: 10px, font-weight 600, letter-spacing: 2px, text-transform: uppercase, color: #9CA3AF

SPECIFIC PAGE FIXES:

Achievements page:
Replace the rainbow category colors (purple, blue, teal, orange, pink, red, cyan) with the user's single primary color throughout. All "0/0" achievement numbers, category labels, and active filter tabs should use the homeowner purple (#3C258E). The category stat grid cards should have a white background with a subtle purple border on the active/selected state only.
Account/Settings page:
Add visible section dividers between each major section (Profile Information, Account Security, Subscription & Billing, Notifications, Referral Rewards, House Transfers, Account Overview). Each section should have a section header with the eyebrow label style. Add 24px of padding between sections. The page currently has no breathing room — this is the primary fix needed.
Contractor Profile page:
Same treatment as Account — add section dividers and breathing room between: Subscription & Billing, Business Information, License Information, Insurance & Compliance, Business Logo, Project Portfolio, Services Offered, Online Presence, About & Experience, Payment Settings, Preview Your Profile, Notifications, Cancel Account.
Messages page:
The "Off" connection status badge should use #DC2626 red with a white background pill, not orange text. The "+ New" button should use the primary color filled button style.
Documents page:
The three tab labels (Documents, Disclosures, Insurance Prep) should use the primary color for the active tab underline and label. The empty state icon color should match the primary color tint.
Referral pages (all three user types):
The social share buttons (Text Message, WhatsApp, Facebook, Twitter) should have consistent styling — border 1.5px solid #E5E7EB, border-radius 10px, background white, color #4B5563. The active/filled share buttons should use the user's primary color.
Support pages:
The "Ask →" button inside the AI assistant box should be the user's primary color, not a generic blue/purple. The FAQ active tab should use the user's primary color background. The "Open a support ticket" button at the bottom should be the user's primary color.
Pricing page:
The three plan cards should have consistent card styling (white, border 0.5px solid #E5E7EB, border-radius 16px). The featured/recommended plan card should have a 2px border in the primary color. The CTA buttons inside each card should use the primary color filled button style.
Onboarding flow:
The progress bar should use a gradient from the primary color (purple) to the secondary (blue). All card headers (Property Profile, Home Wellness Score™) should use the purple gradient matching the homeowner landing page header style. The "Claim this home" CTA card should match the gradient style from the landing page.
All logged-in pages — top nav bar:
The top nav bar currently shows only the logo and notification bell. It should also show the user's name or avatar on the right. The notification bell badge color should use the primary color, not red, unless there are urgent alerts.

CRITICAL RULES:

Do not change any text content, labels, data, or copy
Do not change any routing, API calls, or data logic
Do not change the environmental flag classes .warn .ok .info — used dynamically
Do not remove the sidebar navigation from logged-in pages
Do not change the color system assignment: purple = homeowner, blue = contractor, green = agent
After applying all changes, test every route and confirm the app renders correctly
Apply changes file by file — show me each file as you complete it so I can review before moving to the next

Start with the files that affect the most pages first: the shared CSS/design tokens, then the sidebar component, then page by page in this order: Dashboard → Tasks/Maintenance → Service Records → Documents → Messages → Achievements → Referral → Account → Support → Pricing → then contractor pages → then agent pages → then onboarding/public pages.