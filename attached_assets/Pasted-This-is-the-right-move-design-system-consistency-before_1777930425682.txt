This is the right move — design system consistency before you go any further. Here's the prompt:

---

> I need a full UI consistency audit and update across all pages and screens in this project. The goal is to make every page, modal, and component follow the same design system — the only intentional difference between user paths is the color palette.
>
> **Before making any changes, do the following:**
>
> **Step 1 — Audit every page and list inconsistencies**
> Review every HTML file in the public/ folder and every component in src/. For each one, identify:
> - Font family mismatches (everything should be Inter from Google Fonts)
> - Border radius inconsistencies (cards, buttons, inputs, modals should all match)
> - Button style inconsistencies (padding, font size, font weight, border radius, hover states)
> - Input field inconsistencies (padding, border, border radius, focus state)
> - Card inconsistencies (background, border, border radius, shadow, padding)
> - Modal inconsistencies (overlay, container style, close button position, header treatment)
> - Spacing inconsistencies (section padding, gap between elements)
> - Nav bar inconsistencies (height, logo size, link styles, CTA button style)
> - Footer inconsistencies (background, link styles, layout)
> - Any page that is not loading Inter from Google Fonts
> - Any page using a color that is not in the approved brand palette below
>
> **Step 2 — Define the design tokens**
> Apply these exact values as CSS variables on every page:
>
> Colors:
> --purple: #3C258E
> --purple-deep: #2C0F5B
> --purple-light: #B6A6F4
> --purple-tint: #EEEDFE
> --purple-border: #CECBF6
> --blue: #1560A2
> --blue-deep: #0C3460
> --blue-light: #AFD6F9
> --blue-tint: #E6F1FB
> --blue-border: #B5D4F4
> --green: #079669
> --green-deep: #09694A
> --green-light: #D4EBDE
> --green-tint: #F0FAF4
> --green-border: #A7D7B8
> --gray-100: #F3F4F6
> --gray-200: #E5E7EB
> --gray-400: #9CA3AF
> --gray-600: #4B5563
> --gray-900: #111827
> --white: #FFFFFF
>
> Typography:
> --font: 'Inter', system-ui, sans-serif
> Body: 14px/1.6, color #111827
> H1: 32–52px, weight 700, line-height 1.15
> H2: 24–36px, weight 700, line-height 1.25
> H3: 18–20px, weight 600
> Label/eyebrow: 10–11px, weight 600, letter-spacing 2–3px, uppercase
>
> Spacing:
> --radius-sm: 8px
> --radius-md: 12px
> --radius-lg: 16px
> --radius-xl: 20px
> Section padding: 56px 24px
> Card padding: 20–24px
> Max content width: 880px centered
>
> Components:
> Button primary: padding 13–15px 24–28px, border-radius 12px, font-weight 700, font-size 14–15px
> Button secondary: same radius, border 1.5px solid, background transparent
> Input: padding 12px 16px, border 1.5px solid --gray-200, border-radius 10px, focus border uses primary color
> Card: background white, border 0.5px solid --gray-200, border-radius 16px, box-shadow 0 2px 12px rgba(0,0,0,0.06)
> Modal: border-radius 20px, padding 2px outer wrap, inner border-radius 18px, accent strip 4px tall at top using primary color gradient
> Nav: height 56px, sticky top 0, background white, border-bottom 0.5px solid --gray-200
> Progress bar: height 3px, border-radius 2px, gradient from primary to secondary color
>
> **Step 3 — Color rules by user path**
> Homeowner pages use purple as primary color
> Contractor pages use blue as primary color
> RE Agent pages use green as primary color
> Shared/neutral pages (index-selector, login, etc.) use the full gradient across all three
>
> **Step 4 — Show me the inconsistency report first**
> List every inconsistency found before making any changes. Organize by page. Tell me how many changes are needed per page.
>
> **Step 5 — Wait for my approval**
> After showing me the report, ask me to confirm before applying any changes.
>
> **Critical rules:**
> - Do not change any copy, content, or functionality
> - Do not change any API calls or data logic
> - Do not change any routing
> - Do not change color assignments per user path — purple stays homeowner, blue stays contractor, green stays agent
> - Do not change the environmental flag classes .warn, .ok, .info — these are used dynamically
> - If a component is shared across user paths, it should use CSS variables so the color swaps automatically based on the page context
> - After changes are applied, test every route and confirm the UI renders correctly on both desktop and mobile viewport sizes

---

Once it comes back with the inconsistency report, share it here and I'll help you decide what to approve, what to skip, and what order to tackle it in.